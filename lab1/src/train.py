from lab1.configs import TrainingConfig


def train(config: TrainingConfig) -> None:
    raise NotImplementedError(
        "The Phase 1 task defines training configuration only; training needs real GPU validation."
    )
