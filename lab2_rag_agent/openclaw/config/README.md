# OpenClaw integration boundary

The initial phase only defines controlled tool contracts. OpenClaw configuration,
NemoClaw/OpenShell policy, and a real local agent process are intentionally deferred.
