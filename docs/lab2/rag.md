# RAG Lab 2

`DocumentHit` giữ retrieval score, rerank score, rank, retrieval method và source
metadata. Dense, sparse và hybrid result cần được đánh giá riêng. Semantic result
không được ghi đè exact catalog fact; nó chỉ cung cấp evidence để resolve unknown.
